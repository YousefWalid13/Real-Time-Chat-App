# # .env.development
# # Leave empty so api.js uses "" as BASE_URL → Vite proxy handles /api → backend
# VITE_API_URL=http://localhost:5173

# # If you prefer to bypass the proxy and hit backend directly, use:
# # VITE_API_URL=http://localhost:5000
